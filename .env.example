
# Backend Configuration
BACKEND_PORT=3001

# MongoDB Configuration
MONGODB_URI="mongodb://localhost:27017/seedvault" # Replace with your MongoDB connection string
MONGODB_DB_NAME="seedvault" # Optional: Defaults to 'seedvault' if not set

# JWT Configuration
JWT_SECRET="your-very-strong-and-secret-jwt-key" # Replace with a strong, random secret key

# Seed Phrase Encryption Key (IMPORTANT: MUST be 32 bytes for AES-256)
# Generate a secure random key (e.g., using openssl rand -base64 32) and store it safely.
# DO NOT use the default key in production.
SEED_ENCRYPTION_KEY="default-very-insecure-key-32-bytes"

# Google Generative AI API Key (if using Genkit features)
# GOOGLE_GENAI_API_KEY="your-google-ai-api-key"
